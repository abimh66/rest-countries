<script setup>
import { useDark, useToggle } from '@vueuse/core';
import { useRouter } from 'vue-router';

const router = useRouter();

const isDark = useDark({
  selector: 'html',
});
const toggleDark = useToggle(isDark);
</script>

<template>
  <header
    class="relative flex flex-none justify-between p-5 sm:px-10 font-nunito shadow-lg z-10 dark:bg-dark-blue"
  >
    <h1
      class="font-semibold text-[16px] cursor-pointer"
      @click="router.push('/')"
    >
      Where in the world?
    </h1>
    <div
      @click="toggleDark()"
      class="text-[12px] flex gap-1 items-center cursor-pointer"
    >
      <svg
        xmlns="http://www.w3.org/2000/svg"
        fill="none"
        viewBox="0 0 24 24"
        stroke-width="1.5"
        stroke="currentColor"
        :class="`w-4 h-4 ${isDark ? 'fill-white' : 'fill-none'}`"
      >
        <path
          stroke-linecap="round"
          stroke-linejoin="round"
          d="M21.752 15.002A9.72 9.72 0 0 1 18 15.75c-5.385 0-9.75-4.365-9.75-9.75 0-1.33.266-2.597.748-3.752A9.753 9.753 0 0 0 3 11.25C3 16.635 7.365 21 12.75 21a9.753 9.753 0 0 0 9.002-5.998Z"
        />
      </svg>

      <span>Dark Mode</span>
    </div>
  </header>
</template>
